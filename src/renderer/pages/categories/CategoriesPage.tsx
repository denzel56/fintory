import { PlaceholderPage } from '../../shared/ui/PlaceholderPage'

export function CategoriesPage() {
  return (
    <PlaceholderPage
      description="A future category manager for the simple editable category set used by transactions."
      emptyDescription="Default categories and category editing will be connected after project storage is in place."
      emptyTitle="No categories loaded."
      eyebrow="Categories"
      title="Category management placeholder"
    />
  )
}
