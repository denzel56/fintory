import { PlaceholderPage } from '../../shared/ui/PlaceholderPage'

export function ImportPage() {
  return (
    <PlaceholderPage
      description="A future CSV import workspace for selecting bank exports and reviewing import summaries."
      emptyDescription="File selection, CSV parsing, duplicate detection, and batch history will be added in later phases."
      emptyTitle="No CSV files selected."
      eyebrow="CSV Import"
      title="CSV import placeholder"
    />
  )
}
