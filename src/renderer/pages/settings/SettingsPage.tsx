import { PlaceholderPage } from '../../shared/ui/PlaceholderPage'

export function SettingsPage() {
  return (
    <PlaceholderPage
      description="A minimal place for future app and project preferences that are useful for local CSV-based workflows."
      emptyDescription="Settings will be added only when there is a clear local-first need."
      emptyTitle="No settings are available yet."
      eyebrow="Settings"
      title="App settings"
    />
  )
}
